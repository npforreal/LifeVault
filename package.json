{
  "name": "lifevault",
  "private": true,
  "scripts": {
    "dev": "npm --prefix backend run dev & npm --prefix frontend run dev -- --host 0.0.0.0 --port 5173",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "test": "npm --prefix backend run test"
  }
}
